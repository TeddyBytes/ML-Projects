from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Use wearables' sensors (accelerometer & gyroscope) to classify exercise types.',
    author='TeddyBytes',
    license='MIT',
)
